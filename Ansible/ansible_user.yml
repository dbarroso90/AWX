---
- name: Create ansible user and grant sudo access
  hosts: all

  tasks:
    - name: Create ansible user
      user:
        name: ansible
        password: "{{ 'have' | password_hash('sha512', 'mysecretsalt') }}"
        createhome: yes

    - name: Create ansible sudoers file
      copy:
        dest: /etc/sudoers.d/ansible
        content: 'ansible ALL=(ALL) NOPASSWD: ALL'
        mode: '0440'

    - name: Create SSH directory for Ansible user
      file:
        path: "/home/ansible/.ssh"
        state: directory
        mode: "0700"
        owner: ansible
        group: ansible

    - name: Copy RSA public key
      copy:
        src: "id_rsa_ansible.pub"
        dest: "/tmp/id_rsa_ansible.pub"
        owner: ansible
        group: ansible
        remote_src: false

    - name: Add RSA public key to authorized_keys
      authorized_key:
        user: ansible
        key: /tmp/id_rsa_ansible.pub
        state: present
